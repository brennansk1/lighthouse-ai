# Live validation run — 2026-06-10 (Mac mini, continuous loop session)

Working scratchpad for the `docs/LIVE_TEST_PLAN.md` matrix. Update per row as
measured; fold into PRODUCTION_CHECKLIST + DEV_LOG when each phase completes.

## Session log
- Baseline: 3018 pass / 83 skip → after rebase onto origin (17 docs/UX commits): 3177 pass / 82 skip / 1 ordering-flake (fixed). mypy 0, ruff clean.
- **Bugs found + fixed this session (all committed + pushed):**
  1. soak/smoke dispatch loop built a REAL gateway when Ollama reachable → starved behind RAM gate (`low_free_ram` every tick, 0 jobs dispatched) or would run real models. Fix: `_start_dispatch_loop(offline=True)` pins stub path.
  2. soak leak detector false-positive: fd warmup 7→11 flagged as leak (per-fd probe: pool/kqueue warmup, plateaus at 10). Fix: warmup-prefix skip + absolute-rise floor.
  3. soak hammered live RSS feeds once a minute under --load. Fix: LIGHTHOUSE_AIRGAP default in harness; egress-blocked jobs degrade cleanly (verified).
  4. restic passphrase NEVER reached the binary (no RESTIC_PASSWORD anywhere) — real backups prompt/fail. Fix: env injection (never argv).
  5. restic rc never checked → failed backups logged `backup_ok` / CLI printed "backed up". Fix: ResticError on rc≠0, stdin closed.
  6. hourly backup loop could never succeed on a fresh box (repo never initialized). Fix: turnkey auto-init in _backup_tick.
  7. `litestream restore` without `-config` dies (/etc/litestream.yml) — e2e test + both runbook docs fixed.
  8. my own regression test leaked a dispatch-loop daemon thread across tests (full-suite flake) — inert-thread fix.
- Installed: litestream 0.5.12, restic (brew).

## Matrix status
### Phase 0 — Environment baseline
- 0.1 clean install: ⬜ (needs fresh clone; low priority on this box — dev venv validated 5/30)
- 0.2 offline gates: ✅ full suite green on this hardware (3177/82, mypy 0, ruff clean)
- 0.3 models + doctor: ✅ exit 0, all green; T2 budget 15.47 GB; MoE roles honestly "pages SSD", aux qwen3.5-9b fits
- 0.4 doctor honesty: ✅ (the falsifiable sub-check: 644-perm secrets.toml reported in plain language + exit 1; keyring-absent/low-disk simulations not faked)
- 0.5 first-run path (init → 3-step card → first run): ⬜
### Phase 1 — Retrieval & grounding
- 1.1 `lighthouse eval`: ✅ recall@5 **1.000**, MRR **1.000** (bar ≥0.95), real bge-m3 + FlagReranker, 2026-06-10
- 1.2 reranker lift: ✅ recorded **neutral-on-this-set** (base already at ceiling 1.000/1.000)
- 1.3 contextual lift: ✅ same — ceiling-saturated golden set; lift unmeasurable here, recorded honestly
- 1.4 entailment ≥0.80: 🚫 **BLOCKED** — scorer now requires `minicheck` (cosine fallback removed); not on PyPI; install from the pyproject-prescribed source needs user permission: `uv pip install "git+https://github.com/Liyan06/MiniCheck"` (denied to the agent by permission policy, correctly). ALSO: the `faithfulness` extra (sentence-transformers) is stale — it no longer enables the gate.
- 1.5 zero fabricated citations (10 runs): 🟡 in progress — per-mode E2E running (task bgi4ipsxk) asserts zero-fabrication per artifact
### Phase 2 — Deep research: all ⬜ (needs LLM free; queued behind E2E)
### Phase 3 — Per-mode live acceptance: 🟡 pytest-level E2E running; web-wizard pass ⬜
### Phase 4 — Privacy & security
- 4.1 airgap (lsof variant): ⬜ (queued; soak's airgap leg already showed EgressBlocked on every fetch) · 4.2 egress audit completeness: ⬜
- 4.3 allowlist: ✅ EgressBlocked pre-socket for non-allowlisted host
- 4.4 sandbox red-team: ✅ `lighthouse sandbox redteam` — eicar/zip-bomb/xss/jspdf all blocked
- 4.5 injection gauntlet: 🟡 offline classifier suites green (46); live ingest-gauntlet ⬜
- 4.6 audit tamper: ✅ seeded 5 events, tampered seq 3 → "chain broken at seq(s): [3,4,5]", exit 1
- 4.7 secrets: ✅ keychain-only (sentinel never in data dir or ~/.lighthouse; delete clean)
### Phase 5 — Web UX
- 5.0 ux_sweep: ✅ 9/9 tabs, zero console errors (current main, 2026-06-10)
- 5.1 human walkthrough: needs the user (exception)
- 5.2-5.6: ⬜ (live trace/review/landing/dark-narrow/guide — browser pass with a real run)
### Phase 6 — Endurance & ops
- 6.1 24h soak: 🟡 RUNNING since 2026-06-10 18:52 local (harness task bdtbeeiyk; stub+airgap, --load)
- 6.2 DR kill -9 drill: ⬜ (do when LLM idle) · 6.3 launchd: 🟡 plist `plutil -lint` OK; install+reboot deferred (can't reboot mid-loop)
- 6.4 live calibration ≥20 predictions/1week: ⬜ start positions early so they resolve during the loop
- 6.5 RAM guardrails under concurrent load: ⬜
- outbox chaos @1000: ✅ exactly-once across mid-drain restart, 2.4s (now a permanent test)

## Notes for next wakeup
- Soak: check `bdtbeeiyk` output for loop death / growth; verdict at ~18:52 tomorrow.
- Phase 1 next: `LIGHTHOUSE_REAL_BACKEND=1` retrieval-quality tests + `lighthouse eval`, then faithfulness extra, then the 10-run citation audit (use qwen3.5:9b; watch free RAM, admission queue on).
- Phase 4/5 are cheap wins after that; Phase 2/3 are the long real-model runs — do them serially, smallest model first.
