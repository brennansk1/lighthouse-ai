"""General Web — the universal fallback skill for open-web retrieval.

Searches the open web via SearXNG (self-hosted meta-search) and optionally
fetches full page content through the broker-guarded platform primitives.
Always available; always the final fallback when no specialty source fits.
"""
