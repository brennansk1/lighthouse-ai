# Lighthouse — Project State File (Deep Sweep)

> Maintained by the AI orchestrator. Updated as tasks complete.
> Branch: `claude/jolly-bohr-qq09bq` · Baseline captured: 2026-06-10

---

## 0. Verified Baseline

| Gate | Status | Detail |
|------|--------|--------|
| Tests | **GREEN** | 2,998 passed, 103 skipped (live-network, gated by `LIGHTHOUSE_REAL_BACKEND=1`), 0 failed — 3m01s |
| Ruff | **GREEN** | `ruff check src tests` — all checks passed |
| Mypy | **RED (drift)** | 11 errors in 7 files. CI declares mypy a blocking gate with 0 errors; the branch has drifted. Root cause: `net.guarded_get` types `params` as invariant `dict[str, object]` instead of `Mapping[str, object]`, plus 3 internal `net.py` arg-type errors and 2 `Client | None` narrowing misses in `sources/searxng.py` |

Every change in this sweep must keep tests + ruff green and drive mypy back to 0.

---

## 1. Critical Path Map

*The code most likely to break under refactoring — touch with full ripple analysis only.*

```
cli.py ──► supervisor.py ──► controlplane.py (FastAPI :8765)
                 │
                 ▼
           dispatcher.py ──► modes/registry.py ──► modes/{deepdive,survey,reconstruct,decide,…}
                 │                                        │
                 ▼                                        ▼
           pipeline.py ◄──────────────────────────── gateway.py (all LLM calls, audit, budgets)
                 │                                        │
                 ▼                                        ▼
           rag/ (hybrid BM25+semantic)              governor/ (budgets, loop guard,
                 │                                   injection gate, egress proxy)
                 ▼
           persistence.py / schema.py / paths.py   ◄── imported by 34+ modules (highest blast radius)
```

**Blast-radius tiers:**
- **Tier 1 (extreme care):** `paths.py`, `persistence.py`, `schema.py` (34+ importers); `gateway.py` (13 importers — every mode); `governor/*` (~15 importers)
- **Tier 2 (high care):** `dispatcher.py`, `pipeline.py`, `net.py`/`net_politeness.py` (every source adapter routes through `guarded_get`), `verification/*` (citation-discipline invariant)
- **Tier 3 (contained):** individual `sources/*` adapters, `skills/library/*` (isolated by design — AST import guard, no cross-imports), `modes/*` engines (registry-loaded, independent)

**Security-invariant surfaces (never weaken, always test-first):**
- Egress: `governor/egress_proxy.py` + `net.guarded_get` allowlists + `LIGHTHOUSE_AIRGAP=1` kill switch
- Skills sandbox: AST-level import denylist in `skills/registry.py`
- Citation discipline: zero-fabricated-citations gate in `verification/`
- Audit chain: HMAC-chained tamper-evident log (`verification/audit_chain.py`)

---

## 2. Architectural Principles (enforced during this sweep)

These are extracted from the existing codebase, not invented; the sweep conforms to them.

1. **Sync-first.** The codebase is deliberately synchronous (3 `async def` total, all SSE in `web/routes.py`). No new asyncio in core logic.
2. **httpx only**, always through `net.guarded_get` with an explicit domain allowlist. No raw `httpx.get`, ever `requests`.
3. **Frozen dataclasses** for config/results; Pydantic confined to `web/api.py` (FastAPI boundary). Don't migrate either direction.
4. **structlog everywhere**, context-kwarg style (`log.info("event", key=value)`). No stdlib `logging`.
5. **Domain exceptions + graceful degradation.** Optional heavy deps are lazy-imported; absence degrades (e.g., `RerankerUnavailable` → passthrough), never crashes.
6. **Duplication in `skills/library/` and `sources/` is intentional.** Each skill/source is an isolated, vetted primitive behind the import guard. *Do NOT introduce a shared base class across skills* — it would couple sandboxed units and enlarge the guard surface.
7. **Local-first is the product.** No feature may add a cloud dependency or a default network call. Every new fetch path goes through egress enforcement and the audit log.
8. **Simple UX is a feature.** Customer is a researcher in a regulated setting (legal/clinical/financial), not a developer. New surfaces must be: one CLI verb or one dashboard element, zero-config defaults, plain language. No new tabs, no settings sprawl, no multi-step wizards.

---

## 3. Task Board

Priority = Architectural Impact × Customer Value ÷ Risk. Status: ☐ todo · ◐ in progress · ☑ done · ✗ dropped.

### Phase A — High-Impact / Low-Risk cleanup (start here)

| # | Task | Tier | Files | Status |
|---|------|------|-------|--------|
| A1 | **Restore mypy=0 gate.** Typed `params` as a covariant httpx-shaped `QueryParams` Mapping alias, `data` as `Mapping[str, Any]`, `follow_redirects` as `bool \| UseClientDefault`; fixed `Client \| None` narrowing in `sources/searxng.py` by guarding construction with `if client is None`. mypy: 0 errors / 274 files | [FABLE_REASONING] — `net.py` is Tier-2 blast radius (every adapter) | `net.py`, `sources/searxng.py` | ☑ |
| A2 | **CI runs on feature branches.** Added `claude/**` + `feature/**` push triggers and `workflow_dispatch` | [WORKER_TASK] | `.github/workflows/ci.yml` | ☑ |
| A3 | **Unit tests for `net_politeness.py`** — 31 tests: canonicalization edge cases, robots cache TTL, rate budgets + crawl-delay under injected clocks, dep-free EgressBlocked paths | [FABLE_REASONING] spec → [WORKER_TASK] tests | `tests/test_net_politeness_unit.py` | ☑ |
| A4 | **Unit tests for `provenance.py`** — 43 tests: PROV-O record shape, JSONL log invariants, sidecar determinism, torn-line tolerance. Two cosmetic quirks noted (not fixed): `write_run_sidecar` tmp name doubles `.prov`; torn-line tolerance keyed to physical last line | [WORKER_TASK] | `tests/test_provenance_unit.py` | ☑ |
| A5 | Adjudicate sub-job TODO converted to a tracked deferral: spawn needs loop-guard (`LoopTripped` on contradiction loops) + budget inheritance first — documented in FUTURE_FEATURES.md §6, comment updated in `dispatcher.py` | [FABLE_REASONING] | `dispatcher.py`, `FUTURE_FEATURES.md` | ☑ |

### Phase B — Structural consolidation (ripple analysis required before each)

| # | Task | Ripple summary | Status |
|---|------|----------------|--------|
| B1 | **Mode-engine common spine — shipped as two parts.** (a) `modes/_gate.complete_structured_or` extracts the gate-permit/call/parse/fallback scaffolding (6 hand-rolled blocks removed from survey/reconstruct/decide; `None` is the only fallback sentinel). (b) *The real find:* five engines (debate, quc, exhaustive ×2 call sites, monitor salience) made LLM calls with NO scheduler gate — host-courtesy throttling wasn't uniform. All now take keyword-only `gate=None` and wrap calls; dispatcher + pipeline thread their gates through. Registry `module:function` contract untouched | All `modes/*` + dispatcher + pipeline; suite re-run green (3,069 passed) | ☑ |
| B2 | **Adapter test harness dedup — scoped down on inspection.** The "near-identical scaffolding" claim didn't survive verification: per-skill fixtures are inherently one-liners. Only real duplication was the identical 2-line `broker` fixture in 11 files → moved to `tests/conftest.py` (bespoke brokers in test_ingest/test_egress_fetch shadow it locally) | Test-only | ☑ |
| B3 | ✗ **DROPPED: `SourceAdapter` base class** for `sources/*` — duplication there is the documented safety design (Principle 6). Cost > benefit | — | ✗ |

> Mypy note for B1/C2: gates re-verified after both phases — 0 errors / 274 files.

### Phase C — Customer-facing feature upgrades (simple-UX mandate)

*Candidates derived from what the product already promises but hasn't smoothed. Each is one verb / one surface, no new configuration.*

| # | Feature | Customer value | UX shape | Status |
|---|---------|----------------|----------|--------|
| C1 | **Doctor gap-fill** — *planning correction:* `lighthouse doctor` already existed and was strong (hardware, models, DBs, audit chain, outbox). Filled its three real gaps: disk space (<5 GB = hard issue), a "privacy & secrets" section (airgap state in plain language, secrets backend via new `SecretStore.backend_status()`, hard failure on world-readable `secrets.toml`) | One-shot "is my box ready", readable by a non-developer | Sections added to existing verb; exit code preserved | ☑ |
| C2 | **Streaming synthesis in the dashboard.** `OllamaBackend.chat(on_token=…)` streams JSON-lines; `Gateway(token_sink=…)` forwards (role, job_id, token); dispatcher publishes SSE `synthesis.token` (synthesizer role only — planner/researcher chatter stays out); `JobTrace` renders a live "Writing synthesis…" pane capped at 6 000 chars. Audit unchanged: final-text hash identical to non-streaming path | Long deep-dive runs no longer look frozen | Existing run view, progressive fill, no new controls | ☑ |
| C3 | **`lighthouse audit-egress --summary`** — one-paragraph plain-English verdict naming hosts + call counts; empty log keeps the airplane-mode all-clear | A lawyer or clinician can read the privacy verdict | Flag on existing verb, prose output | ☑ |
| C4 | **First-run card** after `init`: three steps (pull → start → research), dashboard URL, pointer to `doctor`. Bonus fix found while verifying: `init` ignored `$LIGHTHOUSE_DATA_DIR` (every other command honors it) — now consistent | Onboarding funnel | Plain stdout after existing command; no wizard | ☑ |

### Phase D — Verification & close-out

| # | Task | Status |
|---|------|--------|
| D1 | Full gate re-run: pytest (≥2,998 pass, 0 new fail), ruff clean, mypy = 0 errors | ◐ final run below |
| D2 | CHANGELOG.md updated with the sweep's Added/Changed/Fixed | ☑ |
| D3 | Final ripple report appended (§6 below) | ☑ |

---

## 4. Uncertain Patterns (defined before implementation, per directive)

- **Mode-engine extraction shape (B1):** committing to *module-level helper functions* in `modes/_common.py`, not a class hierarchy — the registry's `"module:function"` lazy-load contract and per-mode test isolation are existing patterns this preserves.
- **Streaming (C2):** must stream *through the gateway* so the audit chain still fingerprints the complete output hash — partial-output auditing is the design question; resolve by hashing the final assembled text exactly as the non-streaming path does.
- **pyrate-limiter pin (`<4`):** explicitly out of scope per the pyproject comment — migration is "a deliberate, separately-tested task." Not touched in this sweep.

## 5. Self-Correction Log

- **C1 planning error caught before code:** the task board assumed `lighthouse doctor`
  didn't exist; it did (and was comprehensive). Re-scoped from "build a command" to
  "fill three real gaps" after reading the implementation.
- **B2 scoped down on evidence:** the architecture scan over-reported test-scaffolding
  duplication; direct inspection showed per-skill fixtures are irreducible one-liners.
  Shipped only the genuinely duplicated `broker` fixture move (11 files → conftest).
- **B3 dropped deliberately:** a `SourceAdapter` base class would fight the documented
  isolation design (AST import guard treats each skill/source as a vetted primitive).
- **Test flake fixed, not suppressed:** the audit-egress summary test failed on rich's
  80-column wrapping splitting `example.com (2)` across lines; assertion now normalizes
  whitespace instead of widening the console.
- **Background pytest run masked by a pipe:** `pytest … | tail` made the shell report
  exit 0 on a pytest usage error (`--timeout` not installed); re-ran without the
  masking construct before trusting the baseline.

## 6. Ripple-Effect Notes (final report)

- **`net.py` typing (A1):** ripple = every source adapter's `params` call site. Verified
  by mypy over all 274 files + egress/politeness test suites; `Mapping` covariance means
  no caller changes were needed.
- **Gate threading (B1):** new keyword-only `gate=None` params on `run_debate`,
  `quc.ask`, `run_exhaustive`, `synthesize_tree`, `make_gateway_salience` — additive,
  so the three external callers (dispatcher adapters, pipeline, deepdive debate hook)
  opted in explicitly and all other call sites are unaffected. `gate_ctx(None)` is a
  `nullcontext`, so offline/test behavior is bit-for-bit unchanged.
- **Streaming (C2):** the riskiest surface (gateway, Tier-1) was kept additive — the
  backend only streams when `on_token` is passed, the gateway only passes it when a
  `token_sink` is wired, and the dispatcher only wires one when both bus and gateway
  exist. Injected test backends without the kwarg are untouched (proven by
  `test_no_token_sink_keeps_chat_non_streaming`). Audit chain hashes the assembled
  final text — identical inputs to `_record` either way.
- **Frontend (C2):** one new SSE event name; unknown events were already ignored by
  every other `useEvents` consumer, so older pages are unaffected.

## 7. Final Gate Status

See the commit history on `claude/jolly-bohr-qq09bq` — each phase landed green:
ruff clean, mypy 0/274 files, full suite 3,069+ passed / 0 failed at every commit.
